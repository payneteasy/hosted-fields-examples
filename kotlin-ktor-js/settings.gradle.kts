// The artefact is named here rather than in build.gradle.kts, because the fat jar takes its name
// from the project: `hosted-fields-example-kotlin.jar`, which release.yml and
// e2e-tests/src/apps.ts both name.
rootProject.name = "hosted-fields-example-kotlin"
