# The Gradle daemon's own heap. The build is small; the default would reserve far more.
org.gradle.jvmargs=-Xmx1g

# A configuration cache entry is reused across runs, which is most of the difference between a
# warm `./gradlew test` here and a cold one.
org.gradle.configuration-cache=true
org.gradle.caching=true
